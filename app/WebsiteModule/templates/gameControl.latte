<p n:ifset="$message"><strong>{$message}</strong></p>

<p><a n:href="restart!">Začít novou hru</a></p>

<p n:if="!$isLocked"><strong>Hráč {$currentPlayer}:</strong></p>

{control turnForm}

<p n:if="!$isLocked"><a n:href="toggleBoard!">{if $shouldBoardBePrinted}Skrýt hrací pole{else}Zobrazit hrací pole{/if}</a></p>

{if $shouldBoardBePrinted}<pre>{$board}</pre>{/if}
